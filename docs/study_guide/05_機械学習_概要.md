# 05 機械学習 ── 概要・重要用語集

> **G検定 クイックリファレンス** | 分野：機械学習 | 2026-02-25

---

## 📖 章の概要

機械学習の4種類の学習方法から始まり、各アルゴリズム（線形回帰・SVM・決定木等）、評価指標（混同行列・F値等）、過学習対策までを網羅。

---

## 🗂️ 大項目一覧と要点

### 学習の種類
| 種類 | 特徴 |
|---|---|
| **教師あり学習** | 正解ラベル付きデータで学習。分類・回帰問題に対応 |
| **教師なし学習** | 正解ラベルなし。クラスタリング・次元削減に使用 |
| **半教師あり学習** | 少量のラベル付き＋大量のラベルなしデータを活用 |
| **強化学習** | エージェントが報酬を最大化するよう試行錯誤 |

### 教師あり学習：主要アルゴリズム
| アルゴリズム | 用途 | キーポイント |
|---|---|---|
| **線形回帰** | 回帰 | 最小二乗法。多重共線性に注意 |
| **ロジスティック回帰** | 分類 | シグモイド（2値）/ ソフトマックス（多クラス） |
| **決定木** | 分類・回帰 | ツリー構造、解釈性高、過学習しやすい |
| **ランダムフォレスト** | 分類・回帰 | アンサンブル（バギング）、過学習に強い |
| **SVM** | 分類 | マージン最大化、カーネル法で非線形対応 |
| **K近傍法（KNN）** | 分類 | 近傍K点の多数決 |
| **ナイーブベイズ** | 分類 | 特徴独立を仮定したベイズ分類器 |

### アンサンブル学習
| 手法 | 特徴 |
|---|---|
| **バギング** | 並列学習・多数決。ランダムフォレストが代表例 |
| **ブースティング** | 直列学習・誤り修正。XGBoost・AdaBoostが代表例 |

### 教師なし学習：主要アルゴリズム
| アルゴリズム | 特徴 |
|---|---|
| **k-means法** | k個のクラスタに分類（非階層型） |
| **階層的クラスタリング** | デンドログラムで可視化。ウォード法で分散を最小化 |
| **PCA（主成分分析）** | 分散が最大となる方向に次元を削減 |

### 分類モデルの評価指標
| 指標 | 計算式 | 重視する場面 |
|---|---|---|
| **正解率（Accuracy）** | (TP+TN)/(全数) | バランスの取れたデータ |
| **適合率（Precision）** | TP/(TP+FP) | FP（偽陽性）を減らしたい場面 |
| **再現率（Recall）** | TP/(TP+FN) | FN（偽陰性）を減らしたい場面（医療診断等） |
| **F値（F1-score）** | 適合率と再現率の調和平均 | バランスよく評価したい場面 |
| **AUC** | ROC曲線の下の面積（0〜1、1が理想） | 閾値によらないモデル比較 |

### 回帰モデルの評価指標
| 指標 | 特徴 |
|---|---|
| **MSE** | 誤差の二乗平均。外れ値に大きく影響される |
| **RMSE** | MSEの平方根。単位が元データと同じ |
| **MAE** | 誤差の絶対値平均。外れ値に強い |
| **決定係数（R²）** | 0〜1。1に近いほど良い当てはまり |

### 過学習と対策
| 概念 | 内容 |
|---|---|
| **過学習** | 訓練データへの過剰適応。汎化誤差が増大 |
| **バイアス** | モデルが単純すぎる誤差（未学習） |
| **バリアンス** | モデルが複雑すぎる誤差（過学習） |
| **正則化** | 損失関数に罰則項を追加してモデルの複雑さを抑制 |
| **L1正則化（Lasso）** | 重みをスパースにする（特徴選択効果） |
| **L2正則化（Ridge）** | 重みを小さく保つ（滑らか） |
| **データ拡張** | 加工で学習データを増やし、過学習を防ぐ |
| **スパース化** | 不要な特徴量を削減 |

### モデル選択
| 指標 | 特徴 |
|---|---|
| **AIC** | 複雑さと当てはまりのバランス（小さいほど良い） |
| **BIC** | AICよりデータ数によるペナルティが大きい |
| **オッカムの剃刀** | 精度が同じなら単純なモデルを選ぶ原則 |

---

## ⭐ G検定 最重要キーワード

`教師あり・なし・強化学習` `分類・回帰` `SVM・マージン最大化`
`ランダムフォレスト` `バギング・ブースティング` `XGBoost` `AdaBoost`
`混同行列` `TP/FP/FN/TN` `適合率・再現率のトレードオフ`
`F値` `ROC曲線` `AUC` `MSE・RMSE・MAE`
`過学習・バイアス・バリアンス` `L1正則化（Lasso）` `L2正則化（Ridge）`
`AIC` `BIC` `オッカムの剃刀`
