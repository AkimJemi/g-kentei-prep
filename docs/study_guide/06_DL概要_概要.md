# 06 DL概要 ── 概要・重要用語集

> **G検定 クイックリファレンス** | 分野：DL概要 | 2026-02-25

---

## 📖 章の概要

ニューラルネットワークの基本構造（パーセプトロン・活性化関数・誤差逆伝播法）、CNN・RNNの基礎を扱う章。

---

## 🗂️ 大項目一覧と要点

### ニューラルネットワークの基本
| 用語 | 内容 |
|---|---|
| **ニューロン（ノード）** | 入力の加重和に活性化関数を適用して出力する単位 |
| **単純パーセプトロン** | 入力・出力層のみ。線形分離可能な問題にのみ対応 |
| **多層パーセプトロン（MLP）** | 隠れ層を追加して非線形問題に対応 |
| **隠れ層（中間層）** | 入力と出力の間の層。ここが深いほど「深い」ネットワーク |
| **重みとバイアス** | ニュートワークが学習するパラメータ |

### 活性化関数
| 関数 | 特徴 | 用途 |
|---|---|---|
| **シグモイド** | 0〜1に圧縮。勾配消失問題を起こしやすい | 2クラス出力 |
| **tanh** | -1〜1に圧縮。シグモイドより良いが勾配消失あり | 中間層 |
| **ReLU** | 0以下は0、正値はそのまま出力。現在最も一般的 | 中間層 |
| **Leaky ReLU** | 負値に微小な傾きを与えてニューロンの死滅を防ぐ | 中間層 |
| **ソフトマックス** | 多クラス確率に変換（合計=1） | 多クラス出力 |

### 誤差逆伝播法（バックプロパゲーション）
- 損失関数の勾配を出力層から入力層に向けて計算
- **チェーンルール（連鎖律）**を利用
- これにより全パラメータの勾配を効率的に計算
- 勾配を用いた学習: `w ← w - η × ∂L/∂w`（ηは学習率）

### 勾配消失・爆発問題
| 問題 | 原因 | 対策 |
|---|---|---|
| **勾配消失** | 活性化関数の微分が小さい（シグモイド等） | ReLU使用、残差接続（ResNet） |
| **勾配爆発** | 勾配が層を通るごとに指数的に大きくなる | 勾配クリッピング |

### バッチ処理と学習
| 用語 | 内容 |
|---|---|
| **バッチ学習** | 全データを一度に使って勾配計算 |
| **ミニバッチ学習** | 一部のデータ（ミニバッチ）で勾配計算。一般的手法 |
| **オンライン学習** | データが来るたびに1件ずつ更新 |
| **エポック** | 全訓練データを1周学習すること |

### 正規化・正則化テクニック
| 技法 | 効果 |
|---|---|
| **バッチ正規化（BatchNorm）** | 各バッチの活性化値を正規化。学習安定化・高速化 |
| **ドロップアウト** | 学習時にランダムにニューロンを無効化。過学習防止 |
| **早期停止（Early Stopping）** | 検証誤差が増大し始めた時点で学習を停止 |

### CNN（畳み込みニューラルネットワーク）
| 用語 | 内容 |
|---|---|
| **畳み込み層** | フィルタ（カーネル）を適用してパターンを抽出 |
| **フィルタ（カーネル）** | 特徴を抽出する小さな行列（3×3など） |
| **プーリング層** | 特徴マップのサイズを縮小（最大プーリング等） |
| **特徴マップ** | 畳み込みで得られた出力 |
| **全結合層** | CNNの最後に配置。分類等の出力に使用 |
| **パディング** | 入力の周囲にゼロを追加してサイズを維持 |
| **ストライド** | フィルタを何ピクセルずらして適用するか |

### RNN（再帰型ニューラルネットワーク）
| 用語 | 内容 |
|---|---|
| **RNN** | 系列データ処理。前の状態（隠れ状態）を次の入力に使用 |
| **長期依存問題** | 長い系列で遠い情報を保持できない問題 |
| **LSTM** | 長・短期記憶を分離したゲート機構で長期依存を解決 |
| **GRU** | LSTMを簡略化した高効率版 |

---

## ⭐ G検定 最重要キーワード

`多層パーセプトロン` `活性化関数` `ReLU` `ソフトマックス`
`誤差逆伝播法` `チェーンルール` `勾配消失` `勾配爆発`
`ミニバッチ学習` `エポック` `バッチ正規化` `ドロップアウト` `早期停止`
`CNN` `畳み込み層` `プーリング層` `特徴マップ`
`RNN` `LSTM` `GRU` `長期依存問題`
